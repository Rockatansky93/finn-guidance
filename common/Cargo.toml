[package]
name = "finn-guidance-common"
version = "0.1.0"
edition = "2021"
description = "Shared types and protocol definitions for FINN Guidance"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
