# Rust build artifacts
/target/
**/target/

# Cargo lock for workspace (keep for applications, gitignore for libraries)
# Since this is an application, we keep Cargo.lock

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Field data (local)
field_data/

# SQLite database (machine-specific config, coverage data)
data/coverage.db
data/coverage.db-journal
data/coverage.db-wal
data/coverage.db-shm
pc/data/coverage.db

# Exported AB line JSON (transient, for cross-PC transfer)
data/finn_ab_lines.json

# Old CSV coverage logs (deprecated, replaced by SQLite)
coverage_logs/
